# A股恐惧贪婪指数（FGI）实施方案 · 终稿 V3.8

**版本**：V3.8.6（F3 代理锁定版）  
**更新日期**：2026年7月24日  
**设计原则**：数据免费、逻辑稳健、每日自动更新、可解释  

> **V3.8.6 修订**（2026-07-24，F3 锁定代理）：
> - **F3 真实 API 停用**：`ak.stock_market_fund_flow`（东财主力资金流）间歇性不可达（连接超时），导致 F3 在真实值 ~49 与 proxy ~73 间跳变，FGI 在 46.7-48.8 间不稳定。锁定为纯上证量价代理，不再尝试获取真实数据。
> - **F3 状态变更**：`substituted` → `normal`（proxy 现为主方法而非降级）。`substituted` 枚举保留供未来使用。
> - **chains.py 清理**：删除 `f3_industry_flow` 链，保留 `f3_index`（上证指数 K 线，proxy 所需）。
> - **spec 更新**：F3 定义、数据质量表、局限性声明同步修订。

> **V3.8.5 修订**（2026-07-23，3 轴审计修复）：
> - **P0 #44 修复**：M1/S3 三层缺陷链修齐。① levistock API 在 2017-06-02 前无数据覆盖（CONSTRAINTS #64），该区间 raw_data 标记 NULL；② `zt_backfill` 把空列表误写成 0，改为 skip；③ `rolling_percentile` 在窗口全同值时退化到 1.0，加 NaN guard。历史 2015 股灾 / 2016 熔断 / 2020 疫情等极端事件的 FGI 方向纠正（如 2016-01-04 熔断日从 ~88 → 12.7）。
> - **#45 S2 退化修复**：原滚动 5 年 rank percentile 在 long-term uptrend 数据上退化为常数（中位数 99.4，std=0.008）。改用 log + Z-score + percentile（与 M4 同方法），分布恢复健康（中位数 52.9，std=0.199）。
> - **#46 M4 degraded 状态覆写修复**：M4 calculator 用 last-good-value fallback 时，run() 末尾的无条件 `upsert_status("normal")` 会覆盖 `degraded` 标记。加 `is_degraded` flag 跳过覆写。
> - **#49 MAD 0.85 阈值退化修复**：M1/S3 corr 在 5 年滚动窗口内长期 > 0.85（57% 时间触发动态降权），不再是保险丝而是常数。改为静态权重 S2=0.75/S3=0.25，删除 `_check_m1s3_correlation` 调用与 `correlation_exceed_rate` 健康度惩罚。
> - **#50 F2 周频 stale 修复**：F2 fund_position 是 weekly 数据，但 forward-fill > 30 天仍标 `normal` 是 bug。加 `>7 天` 阈值标 `degraded`，提示数据陈旧。
> - **#51 daily_status.source 覆写修复**：`record_indicator_status` 用 `r.get("source", "")` 覆写 calculator 内部 `upsert_status` 写入的正确 source。改为 `upsert_status_keep_source`（只更新 status/error，不清空 source）。
> - **#52 substituted 状态 spec 定义**：补充 status 枚举语义表。（V3.8.6 起 F3 不再使用 substituted 状态，枚举保留供未来使用。）
> - **#54 raw_data 残留清理**：删除 f1_margin_growth / f3_large_single_inflow / s3_volume / f2_position（旧 chain 名残留）。
> - **recompute 性能优化**：rolling_percentile vectorized 实现（5.5s/d → 0.16s/d），全量重算时间从 47min+ → 15min。

> **V3.8.4 修订**（2026-07-23，数据准确性审计）：
> - **F3 数据源修复（审计 Issue #42）**：原 `stock_fund_flow_industry(symbol='即时')` 是实时快照接口，仅返回当日数据，导致 2808 天历史回填被同一个值污染（只有 2 个 distinct value）。改用 `stock_market_fund_flow` 返回 120 天历史窗口的主力净流入-净额数据。
> - **口径说明**：原"行业汇总"口径由 `stock_fund_flow_industry` Σ(90 行业净额) 计算；新源 `stock_market_fund_flow` 直接给出全市场主力净流入金额。两者均为正负带符号的"主力资金"流量信号，方向语义一致。
> - **raw_data 残留清理（审计 Issue #43）**：删除 6 个 write-only / 未引用字段（m2_bullish_ratio 1529 行 + s2_sentiment/f2_northbound_ratio/m4_deviation/v1_deviation/v2_deviation 各 2-3 行）。

> **V3.8.3 修订**（2026-07-23，PR #31 合并后）：
> - **F1 数据源切换**：SSE `stock_margin_sse`（沪市，已不稳定）→ 东财 `stock_margin_account_info`（沪深合计，全历史稳定）。历史 raw_data 已回填东财口径（3352 行，2012-09 至今）。F1 公式 `融资余额 / 全A总市值` 现两段都用沪深合计，量纲一致。
> - **forward-fill 容错改善**：`MISSING_DAY_LIMIT` 5 → 10（适应 T+1 上报 + 周末 + 节假日的累积延迟）。新增 `elapsed` 字段记录连续 forward-fill 天数：`elapsed=1` 标记 `normal`（仅 T+1 延迟），`elapsed≥2` 标记 `degraded`（真实数据缺口）。`source_date` 字段记录 forward-fill 实际取值日期，便于溯源。
> - **chain 名统一**：m2_market_overview / s2_sentiment / s3_zt_daily（修复历史命名混乱，详见 #30）。
> - **recompute_scores.py 增强**：新增 `--resume` 模式（断点续算）和 `--include-today` 模式（默认 T+1，跳过当日未完成数据）。`FGI_OFFLINE=1` 在 `main()` 内自动设置，不依赖外部环境变量。
> - **F3 公式修正**：`abs()` 反向 bug（审计 H6）—— proxy 与 real data 现都用带符号值，percentile 排序正确反映"大正流入=贪婪 / 大负流出=恐慌"方向。
> - **F3 历史写入**：calculator 现把 proxy_close / proxy_volume 写入 raw_data，offline recompute 可完整重构。

> **V3.8.2 修订**（2026-07-22，基于 M4 数据源实测不可用）：
> - M4 数据源从东财 `index_zh_a_hist`（换手率%）切换至新浪 `stock_zh_index_daily`（成交量 volume）。
> - 根因：东财对指数行情接口全面反爬（akshare issue #7346/#7351），efinance/BaoStock 等替代源在本机网络环境实测均不可用。新浪源稳定 0.2s 秒回，覆盖 2010 至今（实际创业板数据 2016 起）。
> - 公式变更：原 `Z-score(turnover_rate, 60)` → 新 `Z-score(log(volume), 60)`。log 变换让成交量在数量级跨度大的情况下分布接近正态，Z-score 更稳健。
> - 数学等价性依据：换手率 = 成交量 / 流通股本，流通股本慢变 → volume 与 turnover 在 60 日窗口的 Z-score 高度等价（审计 H7 亦确认原实现本质是 volume 代理）。
> - 数据库字段重命名：raw key `m4_turnover` → `m4_volume`；chain 名 `m4_cyb_turnover` → `m4_cyb_volume`。
> - 历史覆盖：2015-至今（脚本拉取起点，新浪源对 sz399006 实际有数据 2016 起）。
> - **维度门槛收紧**：`calculate_fgi` 要求至少 2 个维度有有效得分才产出 FGI（原先仅要求 ≥1）。动机：防止单维度极端值产生误导性 FGI（如 2015 年初仅 M1 涨停池有数据，重归一化后 FGI=100 是异常值）。仅 1 维度时返回 None。

> **V3.8.1 修订**（2026-07-22，基于数据层审计与用户决策）：
> - 删除 S1 涨跌比：其实现与 M2 严格线性（S1 = 2×M2 − 1），数据库实证得分逐日相同，构成重复计权。情绪维度只留 S2、S3，各占 12.5%。指标总数 12 → 11。
> - 维度整体缺失时跨维度权重重归一化，不再使用默认 50 分；**有效维度不足 2 个时不产出**（V3.8.2 修订）。

---

## 一、概述

### 1.1 指数定义

- **名称**：A股恐惧贪婪指数（A-Share Fear & Greed Index，下称 FGI）
- **分值含义**：0 = 极度恐惧（历史底部区域），100 = 极度贪婪（历史顶部区域）
- **更新频率**：交易日盘后自动计算，T 日数据最迟 T+1 日 08:00 前产出

### 1.2 设计目标

1. **全面捕捉情绪**：动量、情绪、估值、资金四大维度，11 个底层指标
2. **数据可落地**：全部指标均可免费获取，核心依赖 AKShare + levistock + zzshare，爬虫类指标备有降级方案
3. **逻辑严谨**：消除指标同源重复，保留利率锚，确保维度内逻辑自洽
4. **工程可靠**：数据库增量更新，异常自动降级并报警，支持长期无人值守运行

### 1.3 版本演进

V3.8 基于 V3.7 深度评审，修正以下核心问题：

- **P1‑1**：删除与 M2/S1 同源的 S2（散户情绪），情绪维度权重重新分配，消除信号重复放大
- **P1‑2**：V1 恢复为 ERP（1/PE – 10年国债收益率），保留利率锚，更准确衡量股债性价比
- **P1‑3**：V2 恢复为 ΔERP Z‑score（250日窗口），与 V1 构成估值水平+变化速度的双指标互补
- **P1‑4**：F3 概念修正为“主力资金板块偏好”，明确定义与历史代理策略，标注不确定性
- **P2**：M1/S4 预期高相关预先标注并预设处理方案；F2 周频滞后、F1 代理偏差补充说明

---

## 二、指标体系（最终版）

### 2.1 指标一览表

| 维度（权重）      | 编号  | 指标             | 权重    | 方向  | 频率  | 数据源                              | 核心处理                          |
| ----------- | --- | -------------- | ----- | --- | --- | -------------------------------- | ----------------------------- |
| **动量**（25%） | M1  | 涨停板家数          | 6.25% | +   | 日   | levistock/AKShare                | 滚动5年百分位                       |
|             | M2  | 散户意愿（上涨家数占比）   | 6.25% | +   | 日   | zzshare open_sentiment           | 滚动5年百分位                       |
|             | M3  | 上证偏离60日均线幅度    | 6.25% | +   | 日   | AKShare 上证指数                     | 滚动5年百分位                       |
|             | M4  | 创业板成交活跃度       | 6.25% | +   | 日   | AKShare 创业板指 (新浪)            | log(volume) Z‑score → 滚动5年百分位             |
| **情绪**（25%） | S2  | 股吧热度           | 12.5% | +   | 日   | zzshare market_hot_sentiment     | 滚动5年百分位                       |
|             | S3  | 涨停封单量          | 12.5% | +   | 日   | levistock/AKShare                | 滚动5年百分位                       |
| **估值**（25%） | V1  | 沪深300风险溢价（ERP） | 12.5% | –   | 日   | AKShare PE + 国债收益率               | 滚动5年百分位（反向）                   |
|             | V2  | ΔERP Z‑score   | 12.5% | –   | 日   | V1衍生                             | 250日Z‑score → sigmoid（负向）     |
| **资金**（25%） | F1  | 融资余额占比         | 8.33% | +   | 日   | AKShare stock_margin_account_info | 滚动5年百分位                       |
|             | F2  | 基金股票仓位         | 8.33% | +   | 周   | AKShare fund_stock_position_lg   | 周频前向填充 → 滚动5年百分位              |
|             | F3  | 主力资金板块偏好       | 8.34% | +   | 日   | 上证指数量价代理（主）            | `price_change × volume` 带符号值 → 滚动5年百分位（不再使用真实 API） |

> **权重说明**：四维度等权（各25%），维度内指标等权。权重直接加总为100%，无需额外归一化。一致性调节因子作为独立后处理步骤，不占用权重。

### 2.2 各指标详细定义

#### M1 涨停板家数

- **原始值**：当日涨停板股票家数（含一字板、炸板后封板）
- **数据源**：levistock `stock_zt_pool_em`（主），AKShare `stock_zt_pool_em`（备，仅历史）
- **处理**：直接对原始值做滚动 5 年百分位。
- **方向**：涨停家数越多 → 得分越高（贪婪）。
- **降级**：levistock 失败 → AKShare（仅限历史日期 < 今日）。
- **注**：与 S3（涨停封单量）源自同一数据源，上线后若两者相关系数 > 0.85，则将 S3 权重减半，差额并入 S2。

#### M2 散户意愿（上涨家数占比）

- **原始值**：`上涨家数 / (上涨家数 + 下跌家数)`
- **数据源**：zzshare `open_sentiment`（主），AKShare 全市场涨跌家数（备）
- **处理**：滚动 5 年百分位。
- **方向**：上涨占比越高 → 得分越高。
- **说明**：本指标衡量市场宽度。（V3.8.1 起 S1 涨跌比已删除：其实现 `(up−down)/(up+down)` 与本指标 `up/(up+down)` 严格线性，得分逐日相同，构成重复计权。）

#### M3 上证偏离 60 日均线

- **原始值**：`(上证收盘价 – 60日均线) / 60日均线`
- **数据源**：AKShare `stock_zh_index_daily` (sh000001)
- **处理**：滚动 5 年百分位。
- **方向**：正偏离越大 → 得分越高。
- **标的选择说明**：选用上证指数而非沪深300，因上证指数在散户认知中代表“大盘”，情绪锚定效应更强。

#### M4 创业板成交活跃度

- **原始值**：创业板指数 (sz399006) 成交量的 log Z‑score（相对 60 日均值与标准差）
- **数据源**：AKShare `stock_zh_index_daily` (sz399006, 新浪源)
- **处理**：log(volume) → 60 日 Z‑score → 滚动 5 年百分位。
- **方向**：成交量显著高于均值 → 投机情绪高涨 → 得分越高。
- **设计说明（V3.8.2 变更）**：原 V3.8 使用东财 `index_zh_a_hist` 的换手率字段，但该接口自 2026-07 起对指数行情全面反爬（akshare issue #7346/#7351），efinance/BaoStock 等替代源实测均不可用。改用新浪稳定的 `stock_zh_index_daily` 成交量端点。由于换手率 = 成交量 / 流通股本（流通股本慢变），volume Z-score 与 turnover Z-score 在数学上高度等价（审计 H7 亦确认原实现本质是 volume 代理）。log 变换让 Z-score 在数量级大的成交量上更稳健（接近正态分布）。

#### S2 股吧热度

- **原始值**：zzshare `market_hot_sentiment` 返回的 `p_close` 字段（市场热度指数收盘值）
- **数据源**：zzshare `market_hot_sentiment`
- **处理**：滚动 5 年百分位。
- **方向**：热度越高 → 得分越高（贪婪）。

#### S3 涨停封单量

- **原始值**：当日所有涨停板股票的封单金额总和（亿元）
- **数据源**：levistock `stock_zt_pool_em`（主），AKShare `stock_zt_pool_em`（备，仅历史）
- **处理**：滚动 5 年百分位。
- **方向**：封单量越大 → 得分越高。
- **降级**：同 M1。
- **注**：与 M1 同源，已预先制定相关性处理方案（见 M1 注）。

#### V1 沪深300风险溢价（ERP）

- **原始值**：`1 / 沪深300滚动市盈率 – 10年期国债收益率`
- **数据源**：AKShare `stock_index_pe_lg` (沪深300) + `bond_china_yield`
- **处理**：滚动 5 年百分位，**反向**（ERP 高 = 股票相对债券便宜 = 恐惧，得分低）。
- **角色**：估值安全垫与股债性价比锚。

#### V2 ΔERP Z‑score（250 日窗口，方向为负向）

- **原始值**：`ΔERP = ERP_t − ERP_{t-1}`
- **处理**：  
  1. 取过去 250 个交易日的 ΔERP 序列，计算 Z‑score。  
  2. 映射：`score = 100 / (1 + e^{Z})`（指数取 +Z 实现负向）。
- **方向**：**负向**（ERP 快速下行 → 股票急涨 → 贪婪，得分高）。
- **说明**：V2 衡量估值变化速度，与 V1 估值水平形成互补。

#### F1 融资余额占比

- **原始值**：`沪深合计融资余额 / 沪深合计全A总市值`（量纲一致：分子分母都用沪深合计）
- **数据源**：AKShare `stock_margin_account_info`（融资余额，沪深合计）+ `stock_account_statistics_em`（市价总值-上海 + 市价总值-深圳）
- **处理**：滚动 5 年百分位。
- **方向**：融资越活跃 → 得分越高。
- **注**：V3.8.3 已从 SSE-only 升级到沪深合计口径，全历史 raw_data 已用东财源回填（3352 行，2012-09 至今）。

#### F2 基金股票仓位

- **原始值**：公募基金股票仓位中位数（百分比）
- **数据源**：AKShare `fund_stock_position_lg`（周频，每周五更新）
- **处理**：  
  1. 周频数据前向填充至日频。  
  2. 对日频序列做滚动 5 年百分位。
- **方向**：仓位越高 → 机构越乐观 → 得分越高。
- **局限**：周频填充在极端行情下信号滞后可达 5 个交易日，已标注于局限性声明。

#### F3 主力资金板块偏好

- **原始值**：上证指数 `price_change × volume`，带符号（正值=净流入代理，负值=净流出代理）。
- **数据源**：上证指数 K 线（AKShare `stock_zh_index_daily` → mootdx/Tencent 兜底），经 `f3_index` 链查询。
- **处理**：  
  1. 取上证指数日线 `close` 计算 `price_change_pct`，乘以 `volume`，得 `flow_magnitude`（带符号，无量纲）。
  2. 该序列经 5 年滚动百分位标准化 → 0–100 得分。
  3. **V3.8.3 修正**：早期实现的 `|abs|` 把大额流出与大额流入打等分，违反"净流入越大→得分越高"语义——已改为带符号值，大正流入=贪婪 / 大负流出=恐慌。
- **方向**：净流入代理值越大 → 得分越高。
- **概念说明**：本指标原用 `stock_market_fund_flow`（东财全市场主力资金日度净流入），该 API 间歇性不可达。V3.8.6 起锁定为纯量价代理——`price_change × volume` 方向正确（放量上涨=贪婪，放量下跌=恐惧），但系统性高估约 24 分（总成交额中散户交易也计入）。全部历史数据用同一方法计算，序列内百分位相对位置有效。

---

## 三、合成方法

### 3.1 得分标准化

- 各指标按 2.2 节方法转化为 0–100 得分。
- 缺失值用最近有效得分填充，最长不超过 10 个交易日。

### 3.2 第一层：维度内聚合

- 动量维度得分 = `(M1 + M2 + M3 + M4) / 4`
- 情绪维度得分 = `(S2 + S3) / 2`
- 估值维度得分 = `(V1 + V2) / 2`
- 资金维度得分 = `(F1 + F2 + F3) / 3`

### 3.3 第二层：加权合成原始 FGI

```
FGI_raw = 0.25 × 动量 + 0.25 × 情绪 + 0.25 × 估值 + 0.25 × 资金
```

（权重合计 100%，无需额外归一化）

**维度缺失处理（V3.8.1, V3.8.2 修订）**：某维度全部指标缺失时，该维度不计入（**不使用默认 50 分**），其余维度权重按比例放大。**有效维度不足 2 个时当日不产出 FGI**（V3.8.2 收紧：单维度信号不足，防止单一极端指标产生误导性 FGI，例如 2015 年初仅 M1 有数据时若按重归一化会得到 FGI=100）。全部维度缺失亦不产出。

### 3.4 第三层：条件一致性调节（后处理）

- 计算每日 **11 个指标得分**的 **MAD**（中位绝对偏差）。
- 计算该 MAD 在滚动 5 年中的**分位数** `p`（0~1）。
- **仅当 FGI_raw < 15 或 FGI_raw > 85 时**，启动调节：
  
  ```
  λ = min( p × 0.6 , 0.3 )
  FGI_final = FGI_raw × (1 − λ) + 50 × λ
  ```
- 其他情况 `FGI_final = FGI_raw`。

**参数说明**：

- `0.6` 为强度系数，`0.3` 为最大回归幅度。
- 须在样本内（2015–2022）验证调节因子的正向贡献，若无效则移除。

---

## 四、数据获取与工程实现

### 4.1 技术栈

- **语言**：Python 3.9+
- **依赖**：`akshare`, `levistock`, `zzshare`, `pandas`, `numpy`, `requests`, `beautifulsoup4`, `scipy`, `sqlite3`, `matplotlib`
- **存储**：SQLite
- **调度**：Linux cron 或 Windows 任务计划，交易日 18:00 触发

### 4.2 数据库设计（EAV 模式）

```sql
CREATE TABLE raw_data (
    date TEXT,
    indicator TEXT,        -- 如 'zt_count', 'fund_position', 'industry_fund_flow' …
    value REAL,
    update_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (date, indicator)
);

CREATE TABLE scores_daily (
    date TEXT PRIMARY KEY,
    M1 REAL, M2 REAL, M3 REAL, M4 REAL,
    S1 REAL, S2 REAL, S3 REAL,  -- S1 已废弃（V3.8.1 删除），列保留兼容历史数据
    V1 REAL, V2 REAL,
    F1 REAL, F2 REAL, F3 REAL,
    FGI_raw REAL, FGI_final REAL
);

CREATE TABLE daily_status (
    date TEXT,
    indicator TEXT,
    status TEXT,  -- 'normal', 'missing', 'degraded', 'substituted'
    PRIMARY KEY (date, indicator)
);
```

**状态枚举语义（V3.8.5 补充）**：

| status      | 含义                                                                     | 示例                                |
|-------------|--------------------------------------------------------------------------|-------------------------------------|
| `normal`    | 当日真实数据获取成功，得分可信                                           | 各 calculator 主数据源正常返回      |
| `missing`   | 当日无有效数据（API 失败 + 无 DB 兜底）                                  | 早期历史段缺失；当日 API 异常       |
| `degraded`  | 数据质量下降但仍参与计算（last-good-value、weekly 数据向前填充>7 天等）  | M4 用 last-good-value；F2 周频滞后  |
| `substituted` | 当日使用替代代理公式计算（非主数据源）                                 | （V3.8.6 起无活跃指标使用；保留枚举供未来使用） |
| `error`     | calculator 内部异常                                                      | code bug；未来使用的兜底枚举        |

### 4.3 每日运行流程

1. 确定最近交易日 T。
2. 逐指标增量更新，写入 `raw_data`，同时更新 `daily_status` 表。
3. 从 `raw_data` 读取足够历史，计算得分及 FGI，写入 `scores_daily`。
4. 容错降级：见各指标降级方案；连续缺失 10 日则剔除，权重分配给同维度其他指标。阈值由 `fgi/config/settings.py:MISSING_DAY_LIMIT` 控制（V3.8.3 起从 5 调至 10，因 T+1 数据延迟 + 周末 + 节假日实际可累积 4-5 自然日，10 个交易日宽容度更符合实际数据上报节奏）。
5. 异常日志 + 报警（企业微信/钉钉 Webhook）。
6. **计算并输出指数健康度**（见 4.5 节），健康度低于 60 时推送标注“数据质量异常，仅供参考”。
7. **FGI 日度变动异常检测**：当日 FGI_final 与前一交易日的绝对差值若超过滚动 5 年日度变动的 99% 分位数，标记“疑似异常”，暂停自动发布，触发人工复核。

**版本兼容与回滚**：每次大版本升级，数据库保留新旧两套得分，并行运行至少 3 个月。`scores_daily` 表增加 `FGI_legacy` 与 `FGI_current` 字段，通过配置开关切换主版本，旧版逻辑保留 6 个月以备回滚。

### 4.4 冷启动方案

- **回填起点**：2015‑01‑01。
- 前 5 年使用扩展窗口；2020 年起固定 5 年滚动窗口。
- 各指标数据起始时间及处理见附录 B。

### 4.5 指数健康度评分

每日输出 0–100 的健康度，基于：

- **正常指标占比 × 50**
- **(1 – 降级占比) × 30**
- **(1 – 相关系数超标率) × 20**  
  健康度低于 60 时，FGI 值旁标注“数据质量异常，仅供参考”。

---

## 五、回测与验证（开发阶段必须完成）

### 5.1 样本分割

- **样本内**：2015–2022（用于参数选择和指标优化）
- **样本外**：2023–2026（纯验证，不作任何调整）  
  样本外若 IC 不显著或分层单调性差，指数不可上线。
- **额外滚动验证**：以 2 年为训练窗、1 年为测试窗，从 2018 年开始滚动，报告平均表现。

### 5.2 分层回测

- 对每一交易日 T，仅用 T 之前数据确定 FGI 的 10 档等分阈值。
- 统计每档未来 5/20/60 日的收益均值和胜率。
- 基准：沪深300、中证500、中证1000 全收益指数。
- 检验单调性，前 3 年（2015–2017）标注参考性有限。

### 5.3 IC 分析

- 每半年用过去 3 年数据计算 FGI 及各指标与未来 20 日收益的 Rank IC。
- 多重检验校正：12 指标 × 3 窗口 = 36 次检验，采用 Bonferroni 校正。
- 输出 IC 均值、标准差、IR（IC 均值/标准差）、胜率。

### 5.4 策略模拟

- **逆情绪定投**：每月定投金额 = 基础金额 × (1 − FGI/100) × 2。
- **极端阈值择时**：
  - 信号触发：FGI 需**连续 3 个交易日** < 15（买入）或 > 85（卖出）。
  - 冷却期：触发买入后，至少持有 **20 个交易日**才允许反向信号。
  - 计入双边 0.3% 交易成本。
- 对比基准：
  - 买入持有
  - 20 日均线择时
  - 60 日均线择时
  - 股债性价比模型（基于 ERP 历史分位阈值）
- 统计年化收益、最大回撤、年换手次数、信号频率。

### 5.5 其他验证

- **F1 方向验证**：计算融资余额占比与未来收益的 IC，默认正向。
- **一致性调节有效性**：对比有/无调节的样本外表现。
- **剔除测试**：逐一去掉单个指标，观察 FGI 整体 IC 变化，IC 降幅 < FGI 整体 IC 的 10% 则进入淘汰观察。
- **M1/S3 相关性处置验证**：若上线后两者相关系数 > 0.85，按预设方案降权，并对比降权前后 FGI 的 IC。
- **F3 代理精度验证**：`price_change × volume` 与真实主力净流入的秩相关性>0.7 即视为方向有效（V3.8.6 起全量用代理，序列内百分位一致）。

---

## 六、上线后的监控与迭代

- **季度相关性检查**：维度内相关 > 0.75、跨维度 > 0.65 触发人工审查。
- **波动率窗口评估**：每季度检查 V2 等波动率类指标的分布漂移，必要时改用 3 年窗口或衰减加权。
- **数据源健康度**：每周报告缺失次数与降级状态。
- **权重定期审视**：每年度可基于过去一年各维度的 Rank IC 表现，在 ±5% 范围内微调维度权重。
- **未来扩展**：  
  - 权重 IC 动态化、多市场版本（V4.0 计划引入 VIX 或恒生指数风险溢价）、引入 ETF 申赎、雪球结构化产品情绪等。

---

## 七、局限性声明

1. **市场结构变化**：5 年窗口可能滞后于注册制、量化扩容等体制变化。波动率类指标建议持续监控。
2. **共线性处理**：依赖季度人工监控，未引入自动正交化。M1 与 S3 同源已预设应对方案。
3. **另类数据风险**：zzshare、levistock 等爬虫数据源可能因网站改版中断。
4. **F1 数据口径**：融资余额与总市值均采用沪深合计口径（V3.8.3 修复，原上交所单边数据已于 2026-07 回填全历史）。
5. **F2 周频滞后**：基金仓位为周频前向填充，极端行情下信号滞后可达 5 个交易日。
6. **F3 代理精度**：全程使用上证量价代理（真实 API 不可达），系统性高估约 24 分。序列内百分位相对位置有效，但横截面与其他指标对比时需注意偏差方向。
7. **历史数据限制**：部分指标早期缺失（见附录 B），冷启动期精度略降。

---

## 附录 A：关键公式速查

- **Z‑score**：`Z = (x – μ) / σ`，μ, σ 为滚动窗口均值与标准差
- **Sigmoid 映射（正向）**：`score = 100 / (1 + e^{-Z})`
- **Sigmoid 映射（负向）**：`score = 100 / (1 + e^{Z})`
- **滚动百分位**：`rank_pct = (rank – 1) / (N – 1) × 100`
- **ERP**：`1/沪深300PE – 10年国债收益率`
- **一致性因子**：  
  `MAD = median(|Xi − median(X)|)`，`p = rolling_percentile(MAD, 5yrs)`  
  仅当 FGI_raw ∈ [0,15) ∪ (85,100]：`λ = min(p×0.6, 0.3)`，`FGI_final = FGI_raw×(1−λ) + 50×λ`
- **健康度**：`正常占比×50 + (1−降级占比)×30 + (1−超标率)×20`

---

## 附录 B：各指标历史数据起始时间及冷启动处理（V3.8 更新）

| 指标        | 数据起始时间                      | 冷启动处理                      |
| --------- | --------------------------- | -------------------------- |
| M1 涨停家数   | 2020 年 (levistock)          | 2015–2019 扩展窗口，权重暂分给 M2–M4 |
| M2 上涨家数占比 | 2015 年 (zzshare)            | 正常                         |
| M3 偏离均线   | 2014 年 (AKShare)            | 正常                         |
| M4 创业板成交活跃度 | 2016 年 (新浪 stock_zh_index_daily) | 正常                    |
| S2 股吧热度   | 2020 年 (zzshare)            | 2015–2019 扩展窗口，权重暂分给 S3 |
| S3 涨停封单   | 2020 年 (levistock)          | 2015–2019 扩展窗口，权重暂分给 S2 |
| V1 ERP    | 2014 年 (AKShare)            | 正常                         |
| V2 ΔERP   | 2014 年 (AKShare)            | 正常                         |
| F1 融资余额   | 2014 年 (AKShare)            | 正常                         |
| F2 基金仓位   | 2017‑12 (AKShare)           | 正常（周频前向填充）                 |
| F3 主力资金   | 2014 年（上证 K 线）               | 全程代理（V3.8.6 起锁定 `price_change×volume`，不再获取真实 API） |

---

**文档结束。**

本方案已修正 V3.7 中的所有 P1 逻辑硬伤，恢复了估值维度的利率锚与变化速度双重结构，消除了情绪维度的同源重复，并明确了各代理指标的局限性。V3.8 可直接交付开发团队进行编码实现。
